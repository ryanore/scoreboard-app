<header class="">
	<div class="">
		<h1>Forgot Password</h1>
	</div>
</header>

<div class="log-in container col-sm-6 col-sm-offset-3 col-md-4  col-md-offset-4">
	
	<div class="">
		<!-- Should this be a container?  seems kind of overzealous on the markup -->
		<div class="col-xs-12">
			<h2>Doh!</h2>
			<p>We'll send you a new temporary password and then you can go change it in your details page. </p>
		</div>
	</div>

	<form class="content-section">   

	{{#if success}}
		<h2>Success!</h2>
		<p>You should get an email any second with a temporary password.  Don't forget to change it when you log in.</p>
		<p><a class="btn btn-primary" href="#login">log in now</a></p>
	{{else}}

		<div class="form-group">			
      <div class="row">
        <div class="col-xs-12 control-group" >
          <label class="control-label">Username</label>
          <input type="text" class="form-control" name="username" placeholder="username" />
        </div>
   		</div>
			<div class="row">
			{{#if errors}}
				<div class=" col-sm-12 control-group">
					<div class="alert alert-warning">
						<ul>
							{{#each errors}}
								<li>{{this}}</li>
							{{/each}}
						</ul>
					</div>
				</div>
			{{/if}}
				<div class=" col-sm-6 control-group">
					<button class="btn btn-primary btn-block" type="submit">Reset Password</button>
				</div>
			</div>
		</div>
	{{/if}}
	</form> 
</div> 
